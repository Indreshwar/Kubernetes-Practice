---
apiVersion: v1
kind: Service
metadata:
  name: external-svc 
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  externalIPs:
    - 192.168.0.24 #can be any IP which is not in use